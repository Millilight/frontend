mutation ResetPassword(
  $user_id: String!
  $new_password: String!
  $token: String!
) {
  resetPasswordUser(
    resetPasswordUserDto: {
      user_id: $user_id
      new_password: $new_password
      token: $token
    }
  ) {
    _id
  }
}
